# AGiXT Environment Configuration
# Copy to .env.agixt and customize for your setup
#
# Usage:
#   cp .env.agixt.example .env.agixt
#   source .env.agixt
#   agixt up

# =============================================================================
# AGiXT Core Settings
# =============================================================================

# API key for AGiXT authentication
AGIXT_API_KEY=agixt-dev-key

# Number of Uvicorn workers (CPU cores for API)
UVICORN_WORKERS=4

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# Database (PostgreSQL)
# =============================================================================

# PostgreSQL password (change in production!)
POSTGRES_PASSWORD=agixt-dev-password

# =============================================================================
# Object Storage (MinIO)
# =============================================================================

# MinIO credentials (change in production!)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# =============================================================================
# LocalAI Integration
# =============================================================================

# LocalAI OpenAI-compatible API endpoint
# LocalAI runs on the host via Nix, accessed from Docker via host.docker.internal
OPENAI_API_BASE=http://host.docker.internal:8080/v1

# API key for LocalAI (typically not required for local)
OPENAI_API_KEY=sk-localai

# Default model for text generation
# See: localai models  (to list available models)
DEFAULT_MODEL=ggml-gpt4all-j

# =============================================================================
# LocalAI Models Path
# =============================================================================

# Where LocalAI stores downloaded models
LOCALAI_MODELS_PATH=$HOME/.local/share/localai/models

# =============================================================================
# AGiXT Rust SDK Settings
# =============================================================================

# AGiXT server URL (Docker service)
AGIXT_URL=http://localhost:7437

# LocalAI URL (Nix service on host)
LOCALAI_URL=http://localhost:8080

# Default agent name for robot commands
AGIXT_AGENT=ros2-agent

# =============================================================================
# AIOS Agent OS Configuration
# =============================================================================
# AIOS (AI Agent Operating System) - User-space agent kernel
# See: https://github.com/agiresearch/AIOS
#      https://github.com/agiresearch/Cerebrum

# AIOS installation directory
AIOS_DIR=$HOME/.local/share/aios

# AIOS Kernel server port
AIOS_PORT=8000

# AIOS Kernel URL (for Cerebrum SDK)
AIOS_URL=http://localhost:8000

# =============================================================================
# AIOS LLM Provider API Keys
# =============================================================================
# Configure in AIOS config.yaml, or export here for convenience
# AIOS supports: OpenAI, Anthropic, Gemini, Groq, Deepseek, HuggingFace, Ollama, vLLM, Novita

# ANTHROPIC_API_KEY=          # Claude models
# GEMINI_API_KEY=             # Google Gemini models
# GROQ_API_KEY=               # Fast inference
# DEEPSEEK_API_KEY=           # Deepseek models
# NOVITA_API_KEY=             # Novita cloud inference
# HF_HOME=$HOME/.cache/huggingface  # HuggingFace cache directory

# =============================================================================
# AIOS Vector Database (ChromaDB or Qdrant)
# =============================================================================

# Vector DB type: chroma or qdrant
AIOS_VECTOR_DB=chroma

# Qdrant settings (if using Qdrant)
# QDRANT_HOST=localhost
# QDRANT_PORT=6333
# QDRANT_API_KEY=

# =============================================================================
# Cerebrum SDK Settings
# =============================================================================
# Cerebrum is the SDK that communicates with AIOS Kernel

# AgentHub URL for downloading/uploading agents
AGENTHUB_URL=https://app.aios.foundation

# ToolHub URL for downloading/uploading tools
TOOLHUB_URL=https://app.aios.foundation

# =============================================================================
# Service Ports (for reference)
# =============================================================================
# AGiXT API:        7437
# AGiXT UI:         3437
# PostgreSQL:       5432
# MinIO API:        9000
# MinIO Console:    9001
# LocalAI:          8080
# AIOS Kernel:      8000
# NATS:             4222
# Prometheus:       9090
# Qdrant:           6333
